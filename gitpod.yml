image:
  file: .gitpod.Dockerfile
tasks:
  - init: |
      cabal update
      stack build
    command: |
      stack test
vscode:
  extensions:
    - haskell.haskell
    - s0kil.vscode-hsx
    - arrterian.nix-env-selector
